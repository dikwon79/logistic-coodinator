NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Python (Flask) route-optimizer service base URL, e.g. http://192.168.0.10:8105
# Server-side only (never exposed to the browser). The dashboard calls it via /api/opt/*.
OPTIMIZER_API_URL=http://localhost:8105
